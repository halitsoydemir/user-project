package com.employee.api.services;

import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.testng.AbstractTestNGSpringContextTests;

/*import static org.testng.Assert.assertNotNull;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.testng.AbstractTestNGSpringContextTests;
import org.testng.annotations.Test;

import com.employee.api.models.Employee;*/

@SpringBootTest
public class EmployeeServiceTest extends AbstractTestNGSpringContextTests {

	/*
	 * @Autowired private EmployeeService employeeService;
	 * 
	 * @Test public void serviceTest() { assertNotNull(employeeService,
	 * "Null employee service"); }
	 * 
	 * @Test public void saveDataTest() {
	 * 
	 * }
	 */
}
